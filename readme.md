# To start server
The port should be:
```
http://localhost:4000/
```
Use this to start the server:
```
docker-compose up
```
or
```
docker-compose up --build --remove-orphans
```
You can check the port with
```
docker ps
```